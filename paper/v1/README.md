# Submission Snapshot

This directory preserves the paper source and compiled PDF as they existed when
the initial version was submitted.

Keep this directory as provenance. Active revisions should be made in `paper/`.
